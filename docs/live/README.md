# Live Documentation (Oz Build)

These documents describe the currently deployed Layout Specification Governance System.

- `COMPILER_README.md`: TXT -> ExportSpec JSON compiler behavior and usage.
- `SPEC_FORMAT.md`: TXT governed spec file format reference.
- `Guidelines.md`: governance constraints and structure discipline.
- `CLOUDFLARE_SETUP.md`: Cloudflare Pages/domain setup and deployment operations.
- `POSITIONS_BACKUP.md`: Position data backup and recovery procedures.

Scope:
- Governed spec source files in `specs/`.
- Active runtime paths (`src/compiler/*`, `src/App.tsx`, `functions/api/positions/`).
- Active deployment at `specs.venterradev.com`.
